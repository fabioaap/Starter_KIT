import { Dashboard } from "./Dashboard";

export default function DashboardPage() {
  return (
    <main className="min-h-screen bg-[color:var(--color-surface-base)] p-[var(--space-layout)]">
      <Dashboard />
    </main>
  );
}
